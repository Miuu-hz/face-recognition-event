# ============================================
# Face Recognition Event System - Environment Variables
# ============================================
# คัดลอกไฟล์นี้เป็น .env และกรอกค่าที่เหมาะสม
# Command: cp .env.example .env
# ============================================

# --------------------------------------------
# Flask Settings
# --------------------------------------------

# CRITICAL: เปลี่ยน SECRET_KEY ใน production!
# Generate ด้วย: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-this-in-production

# Environment: development, production, testing
FLASK_ENV=development

# Debug mode (True/False)
DEBUG=True

# --------------------------------------------
# Server Settings
# --------------------------------------------

# Host and Port
HOST=0.0.0.0
PORT=10000

# --------------------------------------------
# Database Settings
# --------------------------------------------

# SQLite (Development)
DATABASE_PATH=database.db
DATABASE_URL=sqlite:///database.db

# PostgreSQL (Production - uncomment if using)
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# --------------------------------------------
# Google OAuth Settings
# --------------------------------------------

# Path to Google OAuth credentials file
GOOGLE_CLIENT_SECRETS=client_secrets.json

# Allow HTTP for local development (set to 0 in production with HTTPS)
OAUTHLIB_INSECURE_TRANSPORT=1

# --------------------------------------------
# Upload Settings
# --------------------------------------------

# Maximum file upload size in bytes (16MB = 16777216)
MAX_CONTENT_LENGTH=16777216

# Maximum number of selfie images per search
MAX_SELFIE_UPLOADS=3

# --------------------------------------------
# Face Recognition Settings
# --------------------------------------------

# Face matching tolerance (0.0-1.0, lower = stricter)
# Recommended: 0.4-0.6
FACE_TOLERANCE=0.5

# Face detection model: auto, hog, cnn
# - auto: Auto-detect GPU and use cnn if available, otherwise hog
# - hog: Fast, CPU-friendly (recommended for no GPU)
# - cnn: Accurate, requires GPU or powerful CPU
FACE_MODEL=auto

# Batch size for processing images
BATCH_SIZE=20

# Number of jitters for face encoding (higher = more accurate but slower)
NUM_JITTERS=1

# --------------------------------------------
# Logging Settings
# --------------------------------------------

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path
LOG_FILE=app.log

# Maximum log file size in bytes (10MB)
LOG_MAX_BYTES=10485760

# Number of backup log files to keep
LOG_BACKUP_COUNT=5

# --------------------------------------------
# Optional: Background Task Settings
# --------------------------------------------

# (Uncomment if using Celery + Redis)
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# --------------------------------------------
# Optional: Email Notifications
# --------------------------------------------

# (Uncomment if enabling email notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# --------------------------------------------
# Optional: Error Tracking (Sentry)
# --------------------------------------------

# (Uncomment if using Sentry)
# SENTRY_DSN=https://your-sentry-dsn

# ============================================
# Notes:
# ============================================
# 1. ไฟล์ .env จะถูก ignore โดย git (อยู่ใน .gitignore)
# 2. อย่า commit ค่า SECRET_KEY หรือ password จริงลง git
# 3. ใน production ควรตั้งค่า environment variables ผ่านระบบ hosting
# 4. ตรวจสอบว่า client_secrets.json อยู่ในโฟลเดอร์เดียวกับ app.py
# ============================================
